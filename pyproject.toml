[project]
name = "sentinel-cli-v2"
version = "2.0.0"
description = "OMP-inspired ReAct agent with plugin system"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
